
import 'package:flutter/material.dart';

class postPage extends StatelessWidget {
  const postPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('postPage'),
      ),
      body: Center(
        child: Text('Welcome to the postPage!'),
      ),);
  }
}